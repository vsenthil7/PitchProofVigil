fastapi==0.136.3
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
websockets==14.1
python-dotenv==1.2.2
python-multipart==0.0.27  # required by SSO/SAML form endpoints (FastAPI form data)
numpy==2.2.1

# Observability / Arize Phoenix (OpenInference + OpenTelemetry)
# arize-phoenix (server) intentionally NOT installed in the backend image:
# the Phoenix *server* runs as its own container (arizephoenix/phoenix), and the
# backend only exports OTLP spans to it. The server pkg pins a vulnerable
# strawberry-graphql (CVE) and needs sqlean-py (C++ build); neither is needed.
# arize-phoenix==7.9.0  # use the phoenix container, not the in-image server
arize-phoenix-otel==0.6.1
openinference-instrumentation==0.1.21
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp==1.29.0

# Google Gemini
google-genai==1.39.1

# Testing
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0
fakeredis[aioredis]>=2.20.0  # async Redis fakes for rate-limiter unit tests

# Persistence (E2)
sqlmodel==0.0.22
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0
aiosqlite==0.20.0

# Auth & security (E3)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.5.0

# Observability (E4)
prometheus-client==0.21.1
structlog==24.4.0
defusedxml>=0.7.1

# Resilience (E5)
tenacity==9.0.0
pydantic-settings==2.7.0

# ---- Live/optional integrations -----------------------------------------
# These are only imported lazily in real mode (USE_MOCKS=false). They are NOT
# required for mock-mode runtime or the test suite. Install with the live
# extra when deploying against real Phoenix/Arize/Redis/SSO.
# NOTE: `mcp` pulls a newer starlette than FastAPI 0.115 pins; install it in a
# dedicated live image/venv, not the default runtime.
# mcp>=1.0.0,<2.0.0
# httpx-sse>=0.4.0
# redis[asyncio]>=5.0.0
# google-cloud-discoveryengine>=0.11.0
# pysaml2>=7.4.0
# defusedxml>=0.7.0
# cyclonedx-bom>=4.0.0
